<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- Regular (400) -->
    <font
        app:fontStyle="normal"
        app:fontWeight="400"
        app:font="@font/switzer_variable" />

    <!-- SemiBold (600) -->
    <font
        app:fontStyle="normal"
        app:fontWeight="600"
        app:font="@font/switzer_variable" />

    <!-- Bold (700) -->
    <font
        app:fontStyle="normal"
        app:fontWeight="700"
        app:font="@font/switzer_variable" />
</font-family>